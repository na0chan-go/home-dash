---
name: home-dash
description: HomeDash リポジトリでの作業用 skill です。現在のプロダクト目的、実運用で見えている課題、日次・週次の運用ルーティンに沿って、機能提案、不具合修正、ドキュメント更新、更新確認、優先度整理を進めるための判断基準を提供します。
---

# HomeDash

HomeDash は家庭内 LAN で使う「家庭ホワイトボード」です。毎日安心して使えること、運用負荷が低いこと、MVP0 の範囲を守ることを最優先にします。

## 現在の優先事項

新しい提案や実装に入る前に、まずリポジトリ内の記述から現状の優先事項を読み取ります。

- `docs/ROADMAP.md`: `v0.1.x` は機能拡張ではなく運用安定化が中心
- `docs/WEEKLY_REVIEW.md`: 改善案は実際の摩擦から出し、最大 3 件までに絞る
- `docs/OPS_LOG.md`: 日々の困りごと記録が優先度判断の一次情報
- `README.md` と `docs/UPDATE.md`: 安全な更新手順とバックアップ運用も品質要件

リポジトリ内により新しい根拠がない限り、現在の主な課題は次の4点として扱います。

- タブレット利用時の視認性や誤操作の摩擦
- オフライン復帰やスリープ復帰の安定性
- ドキュメントや運用手順の不足
- バックアップやヘルス確認を省略した更新時のリスク

## スコープのガードレール

`AGENT.md` を前提にしつつ、判断は必ず次の制約の内側で行います。

- MVP0 の対象は `notice`、`shopping`、`garbage (today/tomorrow)` のみ
- 天気、在庫、IoT、献立、株価などの「あると便利」領域へ広げない
- 新しい情報源や設定追加より、既存フローの摩擦低減を優先する
- アーキテクチャは `app -> usecase -> domain` と `usecase -> ports <- infra` を守る

依頼内容がスコープ逸脱の可能性を含む場合は、実装前に `docs/TRIAGE.md` と `docs/ROADMAP.md` を見て明示的に確認します。

## 運用リズム

新しい運用フローを発明するのではなく、リポジトリにすでにある運用リズムに従います。

### 日次

利用時の困りごとに関するタスクでは、最初に `docs/OPS_LOG.md` を確認します。

- 少なくとも1つは具体的な摩擦点を拾う
- 抽象的な見た目改善より、家庭内で毎日使う上での影響を重視する

### 週次

優先度整理、ロードマップ見直し、Issue 化の作業では次の流れを基本にします。

1. `docs/OPS_LOG.md` の最新記録を読む
2. 繰り返し出ている問題を少数の論点にまとめる
3. `docs/TRIAGE.md` のラベルと優先度基準を適用する
4. `docs/WEEKLY_REVIEW.md` を使って週次まとめを更新または下書きする
5. 同時に進める改善は 3 件以下に保つ

### 更新 / リリース

デプロイ、更新、ロールバック、リリース準備を扱うときは次を必ず守ります。

- `docs/UPDATE.md` を読み、更新前バックアップと更新後確認を必ず行う
- `docs/RELEASE.md` を読み、バージョニング、変更履歴、タグ運用に従う
- 更新前バックアップは省略しない
- 状態確認では `AUTH_TOKEN` 利用時と未利用時の差を正しく扱う

## 実装の進め方

コード変更や仕様変更では、基本的に次の順番で進めます。

1. その作業が MVP0 の既存体験改善か、運用リスク低減につながるか確認する
2. 必要最小限の関連ドキュメントだけ読む
   - 目的とスコープ: `README.md`, `docs/ROADMAP.md`
   - 優先度判断: `docs/OPS_LOG.md`, `docs/TRIAGE.md`, `docs/WEEKLY_REVIEW.md`
   - 更新とリリース: `docs/UPDATE.md`, `docs/RELEASE.md`
   - 設計ルール: `docs/ARCHITECTURE.md`, `AGENT.md`
3. 振る舞いを変える場合は縦スライスで実装する
4. 新規挙動や不具合修正には対応するテストを追加または更新する
5. ユーザー向けの挙動や運用手順が変わるならドキュメントも更新する

## 検証

通常は軽量な確認を基本にします。

- backend: `go test ./...`
- frontend: `cd web && npm run test:run`

変更内容に応じて、必要なときは次の確認も行います。

- `docker compose up --build` で全体起動確認
- `scripts/update.sh` を触ったときは `./scripts/update_test.sh`
- 認証、バックアップ、status、dashboard に関わる変更では `README.md` または `docs/UPDATE.md` の API 確認手順を使う

## この skill を使うときの出力方針

このリポジトリで計画や実装支援をするときは、次を意識します。

- その作業が今ある摩擦や運用ルーティンとどうつながるか説明する
- スコープ逸脱のリスクは早めに指摘する
- できるだけ小さく、戻しやすい変更を優先する
- 挙動変更がある場合は必要な運用・ドキュメント更新もあわせて触れる

根拠が不足している場合は、前提を明示したうえで、新しい運用を作らず既存ドキュメントの手順に寄せて判断します。
